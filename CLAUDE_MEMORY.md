# CLAUDE_MEMORY.md

本文件记录与本项目相关的全局注意事项，供 Claude Code 在所有会话中参考。

## MCP 工具配置

### WebSearch 工具

**重要**: 在执行网络搜索时，必须使用 `mcp__MiniMax__web_search` 工具，**不要使用原生的 `WebSearch` 工具**。

- 配置文件位置: `~/.claude/settings.json`
- 正确工具: `mcp__MiniMax__web_search`
- 错误工具: `WebSearch` (原生工具，应避免使用)

配置中已启用 `mcp__MiniMax__web_search` 工具。

## 执行策略

### 资源/依赖未找到的处理

**重要**: 当执行过程中遇到资源或依赖未找到的情况时，**不要**：

1. 直接切换到替代技术方案
2. 回滚已实现的技术方案
3. 自动移除已有的功能或代码

**正确的处理方式**:

1. 首先询问用户，告知具体缺失的资源或依赖
2. 等待用户确认后再决定如何处理
3. 如果用户要求修复，再尝试解决方案

## 上下文管理

### 上下文窗口过载处理

**重要**: 当上下文窗口使用超过 50% 时：

1. 每次响应用户输入前，先判断该输入是否与当前上下文会话相关
2. 如果用户输入与当前会话主题无关（例如：完全不同的项目、新的任务请求等），可以建议执行 `/clear` 清理上下文
3. 清理前应告知用户当前上下文使用情况，并征得用户同意

## 项目特定配置

- Flutter SDK 路径: `/Users/dingmaomao/flutter/bin/flutter`
- 项目使用 fvm 管理 Flutter 版本
- Android SDK 路径: `~/Library/Android/sdk`
